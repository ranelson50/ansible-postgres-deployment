---
databases:
  children:
    masters:
      hosts:
        "01":
          ansible_host: 192.168.0.2 # Provide an IP address or hostname here.
    replicas:
      hosts:
        "02":
          ansible_host: 192.168.0.3 # Provide an IP address or hostname here.
  vars:
    ansible_user: "ec2-user"
    ansible_ssh_private_key_file: "/Users/jimmy/.ssh/key.file" # Provide the key file path here.
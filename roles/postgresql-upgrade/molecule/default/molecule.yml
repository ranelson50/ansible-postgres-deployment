---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: postgres
    image: postgres:9.6
    privileged: True
    env:
      POSTGRES_PASSWORD: mysecretpassword
provisioner:
  name: ansible
  lint:
    name: ansible-lint
verifier:
  name: testinfra
  lint:
    name: flake8
